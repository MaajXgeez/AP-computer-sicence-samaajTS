# Generate a list of numbers from 1 to 67 (inclusive)
numbers = [23, 600, 4, 91, 22, 49]  # range stops at 68, so it includes 67

numbers.sort()

print(numbers)

numbers =[23, 600, 4, 91, 22, 49]  # range stops at 68, so it includes 67

# Bubble Sort
#n = len(numbers)
#for i in range(n):
    #for j in range(0, n-i-1):
       # if numbers[j] > numbers[j+1]:
            # swap
        #    numbers[j], numbers[j+1] = numbers[j+1], numbers[j]
